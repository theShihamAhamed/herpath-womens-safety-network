# HerPath Backend

The backend is not initialized yet.

It will be initialized in PR 2 as an independent npm project using:

- Node.js
- Express
- TypeScript
- MongoDB Atlas

PR 1 intentionally contains no backend package manifest, dependencies, source modules, or placeholder module directories.
